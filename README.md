# Programing 수업 소스 코드
## 02_counter
Vanilla HTML, CSS, JavaScript로 만든 Counter
---
## 20260324 React 시작
```shell
npm create vite@latest .
```

### CounterApp
 - `useState()`
 - `onClick={() => set함수()}`
 - `onClick={() => set함수((이전state) => 이전state +1)}`

 ### TodoListAPP\
 - React Component 분리
 - for -> htmlFor, class -> className
 - props
 - `<input id={id} value={} />`, `<label htmlFor={id} />`\
 - `덩어리.map()`
 - `<TodoItem key={} />`
 - `const handleEvent = (event) => {}`
 - `{조건식 ? 참 : 거짓}`
 - `{조건식 && 참}`
 - `{!조건식 && 거짓}`
 - onKeyDown
 - LocallStorage, `useEffect()`